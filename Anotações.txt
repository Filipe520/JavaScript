                                   ------- JavaScript -------

COMANDOS > >  >

window.alert("Cuidado site em manutenção!")   - vai parecer um box de alerta da tela.

window.confirm("Está gostando do site? ") - vai aparecer um box de confirme ou cancelar na tela.

window.prompt("Qual o seu nome? ") - vai aparecer na tela um box perguntando o seu nome.

                            ---- Aula 02 ----

   Para agente fazer comentarios agente pode usar dois tipos de comentarios

PRIMEIRO COMENTARIO.

        // Faz um comentario na mesma linha.
        /* 

        */ faz comentario em varias linhas.

   Além disso agente vai aprender outras coisas nessas aula.

 no JavaScript as variáveis vai ser chamar var e também pode ser chamar de outro nome que é let.

                                Aula 10 - EVENTOS COM MAUSE - DOM

                                     EVENTO - mouseenter

      evento é quando colocar o mause em cima de uma elemento( exemplo - um bloco article ) 
    quando passar o mause em cima dele vai disparar um alerta falando que o mause tá em cima
         mas esse alerta só vai aparecer ser agente falar para mostrar no console.

                                     EVENTO -maousemove

    Vai disparar um alerta falando que o mause tá ser movendo dentro do elemento que tá selecionado.

                                     EVENTO - mousedown

    Quando clicar no elemento vai disparar um alerta é quando solta o mause vai ser outro EVENTO.

                                    EVENTO - mauseup

    Quando solta o clique vai dispara esse evento

                                    EVENTO - click

    Vai ser quando clica rápido.

                                    Evento - mouseout

    Vai ser quando tirar o mause fora do elemento ai vai dispara esse alerta.

			      		DATA é HORA - new date 

    let data = new Date()

    ano com quatro caracteres agente usar o data.getFullYear() é para acessar os elementos de data agente 
usar o get ai já vai aparecer os elementos de date.

					Verificando uma caixa de input

	let nasc = window.document.......
	if (nasc.value.lenght == 0 || Number(nasc.value) > ano)

	oque significar o value? O value vai pegar as informação que tá dentro do campo input
	
	E lenght == 0 Vai analisar ser o compo vai tá fazio [SIM] ou [NÃO] ser tiver fazio vai aparecer
	uma mensagem de ERRO

	E as duas || significar ou igual no python que or  

					para criar tag com javascript

	agente vai ter que colocar dentro de uma variável é depos colocar o comando createElement('img')
	eu tou usando img mas também podia ser qualquer outra tipo h1,h2,h3,strong......
	é depos agente tem que colocar um atributo com classe ou id olhar sor aqui em baixo.

	let img = document.createElement('img')
	img.seAttribute('id', 'foto') dentro da tag img vai atribuir um id com o nome foto.
	
